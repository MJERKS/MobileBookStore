@model MobileBookStore.ViewModels.PreviewViewModel

@{
    ViewBag.Title = "Preview";
}

<h2>@ViewBag.Title</h2>

<div>
    <h4>Book</h4>
	<hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Book.Publisher)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Book.Publisher.CompanyName)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Book.Author)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Book.Author)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Book.Title)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Book.Title)
        </dd>
    </dl>
    
    @foreach (var image in Model.PrevImages)
    {
        <img src="@image" width="260" height="380">
    }
</div>
<p>
    <!--@Html.ActionLink("Edit", "Edit", new { /* id = Model.PrimaryKey */ }) |-->
    @Html.ActionLink("Back to List", "Index")
</p>
